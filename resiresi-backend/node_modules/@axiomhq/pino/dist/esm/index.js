import build from 'pino-abstract-transport';
import { Axiom } from '@axiomhq/js';

var AxiomEventLevel;
(function (AxiomEventLevel) {
    AxiomEventLevel["Trace"] = "trace";
    AxiomEventLevel["Debug"] = "debug";
    AxiomEventLevel["Info"] = "info";
    AxiomEventLevel["Warn"] = "warn";
    AxiomEventLevel["Error"] = "error";
    AxiomEventLevel["Fatal"] = "fatal";
    AxiomEventLevel["Silent"] = "silent";
})(AxiomEventLevel || (AxiomEventLevel = {}));
async function axiomTransport(options) {
    const axiom = new Axiom(options);
    const dataset = options.dataset;
    return build(async function (source) {
        for await (const obj of source) {
            const { time, level, ...rest } = obj;
            const event = {
                _time: time,
                level: mapLogLevel(level),
                ...rest,
            };
            axiom.ingest(dataset, event);
        }
    }, { close: async () => await axiom.flush() });
}
// See https://github.com/pinojs/pino/blob/master/docs/api.md#loggerlevel-string-gettersetter
const mapLogLevel = (level) => {
    if (typeof level === 'string') {
        return level;
    }
    if (level <= 10) {
        return AxiomEventLevel.Trace;
    }
    if (level <= 20) {
        return AxiomEventLevel.Debug;
    }
    if (level <= 30) {
        return AxiomEventLevel.Info;
    }
    if (level <= 40) {
        return AxiomEventLevel.Warn;
    }
    if (level <= 50) {
        return AxiomEventLevel.Error;
    }
    if (level <= 60) {
        return AxiomEventLevel.Fatal;
    }
    return AxiomEventLevel.Silent;
};

export { AxiomEventLevel, axiomTransport as default, mapLogLevel };
//# sourceMappingURL=index.js.map
