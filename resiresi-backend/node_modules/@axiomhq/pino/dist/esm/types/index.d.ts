/// <reference types="node" />
import build from 'pino-abstract-transport';
import { ClientOptions } from '@axiomhq/js';
export declare enum AxiomEventLevel {
    Trace = "trace",
    Debug = "debug",
    Info = "info",
    Warn = "warn",
    Error = "error",
    Fatal = "fatal",
    Silent = "silent"
}
export interface Options extends ClientOptions {
    dataset: string;
}
export default function axiomTransport(options: Options): Promise<import("stream").Transform & build.OnUnknown>;
export declare const mapLogLevel: (level: string | number) => string;
//# sourceMappingURL=index.d.ts.map