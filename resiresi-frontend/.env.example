# The ResiResi reservation API — run it locally first:
#   cd resiresi-backend && docker compose up
# It is an open origin — no key required.
RESIRESI_API_URL=http://localhost:8080

# Optional overrides for the walkthrough page:
# RESIRESI_REPO_URL=https://github.com/jayjaychicago/rr
# RESIRESI_PROXY_NAME=resiresi

# --- Only needed once the tester adds the widgets (Part B). Server-only. ---
# APIBLAZE_CP_KEY=
